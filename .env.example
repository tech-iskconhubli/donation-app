# Razorpay Configuration
# Get these from your Razorpay Dashboard: https://dashboard.razorpay.com/app/keys

# Razorpay Key ID (Public key - safe to use in frontend)
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_your_key_id_here

# Razorpay Key Secret (Private key - server-side only, NEVER expose in frontend)
RAZORPAY_KEY_SECRET=your_secret_key_here

# Optional: Data directory path (defaults to ./data)
# DATA_DIR=/path/to/data/directory
